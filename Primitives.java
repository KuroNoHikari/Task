class Primitives {
    public static void main(String[] args) {
        char w = 'w', r = 'r', Di = 'd';
        byte zero = 0;
        int tree = 3;
        short EL = 1;
        float two = 2.0f;
        String output = "H" + tree + EL + EL + zero + " " + w + zero + r + EL + Di + " " + two + " " + true;
        System.out.println(output);
    }
}